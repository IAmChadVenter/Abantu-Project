@model Abantu_System.Models.AbantuMember

@{
    ViewBag.Title = "ViewDeactRequest";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

@{
    var imgUrl = Url.Content("~/Content/images/profile/" + Model.FirstName + "_" + Model.Surname + ".png") + "?time=" + DateTime.Now.ToString();
}
<h2 style="text-align:center;">Member profile deactivation request</h2>
<div class="container">
    <h2 style="text-align:center;">Request by @Model.FirstName @Model.Surname</h2>
    <hr />
    <h3 class="list-group-item-heading">Deactivation request details</h3>
    <br />
    <ul class="list-group">
        <li class="list-group-item"><img src="@imgUrl" alt="pic_of_@Model.FirstName @Model.Surname" height="250" width="250" /></li>
        <li class="list-group-item"><b>Full Name :</b> @Model.FirstName @Model.Surname</li>
        <li class="list-group-item"><b>Reason for deactivation request: </b> @Model.DeactReason</li>
    </ul>
</div>
@if (Request.IsAuthenticated)
{
    <p>
        @Html.ActionLink("Approve Deactivation", "InitDeactivation", new { id = Model.Member_ID }, new { @class = "btn btn-primary" }) |
        @Html.ActionLink("Back to List", "AllDeactivations", null, new { @class = "btn btn-primary" })
    </p>
}

